# Minimal deps to run the API only (no prophet/xgboost needed for /summary, /timeline, /crisis-events)
pandas>=2.0.0
numpy>=1.24.0
duckdb>=0.9.0
requests>=2.28.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
psycopg2-binary>=2.9.0
